# HanaAgent

HanaAgent entry point for the llm-wiki reference bundle.

Install or update with:

```bash
bash install.sh --platform hanaagent --target-dir ~/.hanako/skills/llm-wiki
```

Use this entry when the skill is managed from the HanaAgent environment rather than a legacy platform package.
