{
  "name": "hana-code-atlas",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:python": "cd pipeline && python3 -m pytest -q",
    "test:all": "npm test && npm run test:python",
    "pack:assets": "node scripts/pack-ua-assets.mjs",
    "pack:plugin": "node scripts/package-plugin.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
